# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
dist/
build/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Local configuration / secrets
.env
.env.*
!.env.example

# Local banned-pattern list: must never be committed
.git-banned-patterns

# Local command-to-judgement table for the PreToolUse gate: names this
# installation's hosts, wrappers and schedulers, so it stays out of the repo
.mashu-guard-actions

# Data and intermediates
data/
*.dump
*.sql.gz
*.db
*.sqlite3

# Capture agent log: what the nightly run said, per machine
.capture.log

# Editor / OS
.DS_Store
.vscode/
.idea/

# Claude Code local settings
.claude/settings.local.json

# Database dumps (v1 archive and any later backups)
backups/
